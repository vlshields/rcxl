% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_xlsx.R
\name{read_xlsx}
\alias{read_xlsx}
\title{Read an xlsx worksheet}
\usage{
read_xlsx(
  path,
  sheet = 1L,
  col_names = TRUE,
  trim_ws = TRUE,
  range = NULL,
  skip = 0L,
  n_max = Inf,
  col_types = NULL,
  na = "",
  name_repair = c("unique", "minimal", "check_unique")
)
}
\arguments{
\item{path}{Path to an xlsx file. Tilde expansion is applied.}

\item{sheet}{Worksheet to read, as a 1-based index or a sheet name.
Overridden when \code{range} names a sheet.}

\item{col_names}{\code{TRUE} to use the first row of the read area as column
names, \code{FALSE} to number them \code{V1}, \code{V2}, ..., or a character vector of
names, one per sheet column including any skipped via \code{col_types} (the
skipped names are dropped from the result).}

\item{trim_ws}{Trim leading and trailing whitespace from string cells and
column names?}

\item{range}{A1-style cell range to read: \code{"B3:D87"}, a single cell
\code{"B3"}, whole columns \code{"B:D"}, or whole rows \code{"3:87"}, optionally
sheet-qualified as \code{"Sheet1!B3:D87"} or \code{"'My Sheet'!B3:D87"}. When
given, \code{skip} and \code{n_max} are ignored. An axis the range bounds is read
exactly, blank cells included; an open axis is trimmed to the sheet
extent.}

\item{skip}{Number of rows to skip before reading anything. Ignored when
\code{range} is given.}

\item{n_max}{Maximum number of data rows to read. The column name row
does not count. Ignored when \code{range} is given.}

\item{col_types}{\code{NULL} to guess every column, or a character vector of
\code{"guess"}, \code{"skip"}, \code{"logical"}, \code{"numeric"}, \code{"date"}, \code{"text"} or
\code{"list"}. A single value applies to every column; otherwise supply one
entry per sheet column, counting \code{"skip"} columns. A \code{"list"} column
keeps each cell's own type: length-one numeric, \code{POSIXct}, logical or
character elements, with blank cells as logical \code{NA}.}

\item{na}{Character vector of strings to read as \code{NA}. Matched against
whitespace-trimmed cell text; entries that parse as numbers also blank
numeric and date cells holding that value.}

\item{name_repair}{How to resolve column names in the result. \code{"unique"}
suffixes repeated names via \code{\link[=make.unique]{make.unique()}} with separator \code{"_"},
\code{"minimal"} keeps names as read, \code{"check_unique"} errors on any repeat.}
}
\value{
A \code{data.frame} with one column per worksheet column read.
}
\description{
Reads one worksheet from an xlsx file into a data frame. Column types are
guessed from the cells unless overridden with \code{col_types}.
}
\details{
A guessed column becomes character if it holds any string cell, logical if
it holds only booleans, \code{POSIXct} if it holds date cells and no plain
numbers, and numeric otherwise. Every cell in the read area informs the
guess. Columns with only blank cells become logical \code{NA}. Date cells are returned as \code{POSIXct} in UTC; both the 1900
and 1904 date systems are handled, including the nonexistent 29 Feb 1900
that the 1900 system counts.

Cells that cannot be coerced to a type forced through \code{col_types} become
\code{NA}, with a warning giving the count and the first offending cell.
}
\examples{
types <- system.file("extdata", "types.xlsx", package = "rcxl")
str(read_xlsx(types))

# read a rectangle rather than the whole sheet
read_xlsx(types, range = "A1:C3")

# force every column to character instead of guessing
str(read_xlsx(types, col_types = "text"))

# a sheet other than the first, by name or by position
multi <- system.file("extdata", "multisheet.xlsx", package = "rcxl")
read_xlsx(multi, sheet = "Beta")
}
\seealso{
\code{\link[=read_xlsx_all]{read_xlsx_all()}} to read several sheets in one workbook pass;
\code{\link[=xlsx_sheets]{xlsx_sheets()}} for the sheet names.
}
